using System;
using System.Collections.Generic;
/*
public class Node<T>
{
    public T data;
    public List<Node<T>> nodes = new List<Node<T>>();

    public Node(T data)
    {
        this.data = data;
    }
}

public class Tree<T>
{
    public Node<T> root;

    public Tree(T data)
    {
        root = new Node<T>(data);
    }
}

public class TestTree
{
    
    public static void Main(string[] args)
    {
        Tree<int> tree = new Tree<int>(10);
        tree.root.nodes.Add(new Node<int>(3));
        tree.root.nodes.Add(new Node<int>(4));
        tree.root.nodes.Add(new Node<int>(10));
        tree.root.nodes.Add(new Node<int>(7));
        tree.root.nodes.Add(new Node<int>(17));
        tree.root.nodes[1].nodes.Add(new Node<int>(17));
        tree.root.nodes[2].nodes.Add(new Node<int>(17));
        tree.root.nodes[3].nodes.Add(new Node<int>(17));
        tree.root.nodes[3].nodes[0].nodes.Add(new Node<int>(1));
    }
    
}
*/